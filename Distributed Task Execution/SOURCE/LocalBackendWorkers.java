import java.util.HashMap;
import java.io.*;

public class LocalBackendWorkers {
	public static void main(String args[])
	{
	public static Queue LocalBackendWorkers(Queue localqueue) {	
	    String no_threads=this.get(args[2]);

	        File f = new File(Client_AWS.get(args[4]))	
	         BufferedReader bufferedreader = new BufferedReader(new FileReader(f));	
    	while(!localqueue.isEmpty()){
    			ConcurrentHashMap<String, Integer> work =  q.deQueue();
  	    		
    	}
    	while(bufferedreader.readLine() != null)
    	{	
       	SQS_service.process_queue();
    	return SQS_service.clientacknowledgementQueue;
   		}
}
}